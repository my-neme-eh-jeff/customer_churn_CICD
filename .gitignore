# Python
__pycache__/
*.pyc
.venv/

# Models
models/

# DVC cache
/tmp/dvc*

# MLflow
mlruns/
mlartifacts/

# IDE
.idea/
.vscode/

# Environment variables (secrets — never commit)
.env
.env.local

# DVC outputs (regenerated by `dvc repro`)
metrics.json

# Tenant config — user-specific (cloud project, region). Written by `make setup`.
configs/tenant.yaml

# Local narrative / brainstorm / TODO notes — kept on disk, never committed
BRAINSTORM.md
e2e.md

# Claude Code local settings
.claude/settings.json

# >>> atlan-builders >>>
# Managed by atlan-core — edit inside this block and it will be preserved
# only until the next bootstrap tick. Add your own rules outside the block.
.atlan/journal/pending.jsonl
.atlan/journal/.drafts/
.atlan/cache/
# <<< atlan-builders <<<
.specstory/
.claude/
lessons.md
mlflow.db
mlflow_artifacts/

# Local-only notes (do not commit)
NOTES.md

# Demo trajectory plot — regenerated per run via `make plot-trajectory`
auto_experiment/trajectory.png
